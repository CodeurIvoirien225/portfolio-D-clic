
/* projets */
document.getElementById("voirProjets").addEventListener("click", function(event) {
    event.preventDefault(); // Empêcher le comportement par défaut du lien

    // Sélectionner la section cible
    const section = document.getElementById("projects");
    

    // Faire défiler la page en douceur jusqu'à la section
    section.scrollIntoView({ behavior: "smooth" });
});


/* projets */
document.getElementById("voirprojets").addEventListener("click", function(event) {
    event.preventDefault(); // Empêcher le comportement par défaut du lien

    // Sélectionner la section cible
    const section = document.getElementById("projects");
    

    // Faire défiler la page en douceur jusqu'à la section
    section.scrollIntoView({ behavior: "smooth" });
});


/* home */
document.getElementById("accueil").addEventListener("click", function(event) {
    event.preventDefault(); // Empêcher le comportement par défaut du lien

    // Sélectionner la section cible
    const section = document.getElementById("home");

    // Faire défiler la page en douceur jusqu'à la section
    section.scrollIntoView({ behavior: "smooth" });
});


/* skills */
document.getElementById("skill").addEventListener("click", function(event) {
    event.preventDefault(); // Empêcher le comportement par défaut du lien

    // Sélectionner la section cible
    const section = document.getElementById("skills");

    // Faire défiler la page en douceur jusqu'à la section
    section.scrollIntoView({ behavior: "smooth" });
});

/* about */
document.getElementById("abouts").addEventListener("click", function(event) {
    event.preventDefault(); // Empêcher le comportement par défaut du lien

    // Sélectionner la section cible
    const section = document.getElementById("about");

    // Faire défiler la page en douceur jusqu'à la section
    section.scrollIntoView({ behavior: "smooth" });
});

/* contact */
document.getElementById("contacts").addEventListener("click", function(event) {
    event.preventDefault(); // Empêcher le comportement par défaut du lien

    // Sélectionner la section cible
    const section = document.getElementById("contact");

    // Faire défiler la page en douceur jusqu'à la section
    section.scrollIntoView({ behavior: "smooth" });
});